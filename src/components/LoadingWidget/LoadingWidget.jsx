import { StyledLoadingWidget } from '.'

const LoadingWidget = () => {
  return (
    <StyledLoadingWidget >
      Loading up Dashboard...
    </StyledLoadingWidget>
  )
}

export default LoadingWidget
